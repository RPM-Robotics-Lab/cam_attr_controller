package irp_sen;

/**
 * IRAP sensor data: VLP-16 packet
 * @brief VLP-16 packet
 */

struct vlp16_packet_t 
{
    int64_t utime;      //!< sensor timestamp in utime [usec]

    byte data[1206];    //!< velodyne raw packet
}
